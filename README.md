# Zero Mobile Releases

This public repository contains only the signed Zero Mobile APK and signed, non-secret distribution metadata. It contains no application source, account credentials, provider tokens, or developer configuration.
